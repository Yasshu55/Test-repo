name: Failing Test

on:
  push:
    branches: [main]
  workflow_dispatch: # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: This step will fail
        run: |
          echo "Starting tests..."
          python -c "import nonexistent_module"  # This will fail!
